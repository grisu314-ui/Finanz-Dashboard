# Development and test dependencies; not installed in the image.
-r requirements.txt

pytest==9.1.1       # test runner

# --- transitive dependencies of pytest ---
iniconfig==2.3.0
pluggy==1.6.0
Pygments==2.21.0
